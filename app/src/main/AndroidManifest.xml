<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dheo.myclonewa">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name="com.example.dheo.myclonewa.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.dheo.myclonewa.main.MainActivity"/>
        <activity android:name=".welcoming.WelcomingActivity"/>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".register.RegisterActivity" />
    </application>

</manifest>